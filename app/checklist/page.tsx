import { ComingSoon } from "@/components/ComingSoon";

export default function ChecklistPage() {
  return (
    <ComingSoon
      title="Checklist"
      note="Todavía no está construido — viene justo después de dejar el catálogo funcionando con datos reales."
    />
  );
}
