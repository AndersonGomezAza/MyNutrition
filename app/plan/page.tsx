import { ComingSoon } from "@/components/ComingSoon";

export default function PlanPage() {
  return (
    <ComingSoon
      title="Generar plan"
      note="El generador (presupuesto + exclusiones + tienda -> lista de compras y menú de 7 días) todavía no está construido."
    />
  );
}
